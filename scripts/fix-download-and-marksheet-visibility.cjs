const fs = require('fs');
const studentPath = 'app/admin/students/[studentId]/page.tsx';
let s = fs.readFileSync(studentPath, 'utf8');

s = s.replace(/const downloadMarksheet=async\(exam:Exam\)=>\{[\s\S]*?\n const printReceipt=/, 'const downloadMarksheet=async(exam:Exam)=>{\n  if(!student)return;\n  const w=printResult(exam,false);\n  if(!w)return;\n  try{\n   const {jsPDF}=await import("jspdf");\n   const html2canvas=(await import("html2canvas")).default;\n   await new Promise(resolve=>{if(w.document.readyState==="complete")resolve();else w.addEventListener("load",()=>resolve(),{once:true})});\n   await new Promise(r=>setTimeout(r,500));\n   const el=w.document.querySelector(".sheet") as HTMLElement|null;\n   if(!el)throw new Error("Marksheet could not be prepared.");\n   const canvas=await html2canvas(el,{scale:2,useCORS:true,backgroundColor:"#ffffff",logging:false});\n   const pdf=new jsPDF({orientation:"portrait",unit:"mm",format:"a4"});\n   const margin=7;\n   const ratio=Math.min((210-margin*2)/canvas.width,(297-margin*2)/canvas.height);\n   pdf.addImage(canvas.toDataURL("image/png"),"PNG",margin,margin,canvas.width*ratio,canvas.height*ratio);\n   const safeName=student.student_name.trim().replace(/[^a-zA-Z0-9]+/g,"_").replace(/^_|_$/g,"")||"Student";\n   pdf.save(safeName+"_Marksheet.pdf");\n   w.close();\n  }catch(e){\n   console.error(e);\n   w.close();\n   alert("PDF download failed. Please use Print Marksheet and choose Save as PDF.");\n  }\n };\n const printReceipt=');

s = s.replace('.brand{display:flex;align-items:center;gap:18px;border-bottom:4px double #0a7139;padding-bottom:16px}', '.brand{display:flex;align-items:center;gap:18px;border:2px solid #075c2d;background:#075c2d;padding:14px 16px;color:#fff;border-radius:10px}.brand h1{color:#fff!important}.brand .urdu{color:#f4d77b!important}.brand p{color:#e8f4ec!important}');
s = s.replace('.label{font-size:9px;text-transform:uppercase;letter-spacing:.12em;color:#64756b;font-weight:800}', '.label{display:inline-block;background:#075c2d;color:#fff;border-radius:4px;padding:3px 6px;font-size:9px;text-transform:uppercase;letter-spacing:.1em;font-weight:900}');
s = s.replace('.value{font-size:13px;font-weight:800;margin-top:4px}', '.value{font-size:14px;font-weight:900;margin-top:5px;color:#102219}');
s = s.replace('.table th,.table td{border:1px solid #c9d6ce;padding:9px 10px;font-size:12px}', '.table th,.table td{border:1px solid #b9c9c0;padding:10px 11px;font-size:13px;line-height:1.35}');
s = s.replace('.result-banner{margin-top:16px;border:2px solid #075c2d;padding:12px;text-align:center;font-size:18px;font-weight:900;color:#075c2d}', '.result-banner{margin-top:16px;border:2px solid #075c2d;background:#075c2d;color:#fff;padding:12px;text-align:center;font-size:19px;font-weight:900;border-radius:6px}');
s = s.replace('.studentbox{margin-top:16px;padding:14px;background:#f4f8f5;border:1px solid #d6e4da}', '.studentbox{margin-top:16px;padding:14px;background:#f4f8f5;border:1px solid #d6e4da;font-size:14px;line-height:1.6}');

s = s.replace('const printResult=(exam:Exam)=>{if(!student)return;', 'const printResult=(exam:Exam,autoPrint=true)=>{if(!student)return null;');
s = s.replace('printWindow(`${exam.exam_name} Marksheet`,', 'return printWindow(`${exam.exam_name} Marksheet`,');
s = s.replace('function printWindow(title:string,body:string){const w=window.open("","_blank","width=1100,height=900");if(!w)return;', 'function printWindow(title:string,body:string,autoPrint=true){const w=window.open("","_blank","width=1100,height=900");if(!w)return null;');
s = s.replace('window.print()},350)', 'if(autoPrint)window.print()},350)');
s = s.replace('body></html>`);w.document.close()}', 'body></html>`);w.document.close();return w}');
fs.writeFileSync(studentPath, s, 'utf8');

const adminPath = 'app/admin/results/page.tsx';
let a = fs.readFileSync(adminPath, 'utf8');
a = a.replace('w.document.close();}\n\nexport default function AdminResultsPage(){', 'w.document.close();if(download){setTimeout(async()=>{try{const {jsPDF}=await import("jspdf");const html2canvas=(await import("html2canvas")).default;const pages=Array.from(w.document.querySelectorAll(".page")) as HTMLElement[];const pdf=new jsPDF({orientation:"portrait",unit:"mm",format:"a4"});for(let i=0;i<pages.length;i++){if(i)pdf.addPage();const canvas=await html2canvas(pages[i],{scale:2,useCORS:true,backgroundColor:"#fff",logging:false});const margin=7;const ratio=Math.min((210-margin*2)/canvas.width,(297-margin*2)/canvas.height);pdf.addImage(canvas.toDataURL("image/png"),"PNG",margin,margin,canvas.width*ratio,canvas.height*ratio)}const firstName=items[0].student.student_name.trim().replace(/[^a-zA-Z0-9]+/g,"_").replace(/^_|_$/g,"")||"Students";pdf.save(firstName+(items.length>1?"_Results":"_Marksheet")+".pdf");w.close()}catch(e){console.error(e);w.close();alert("PDF download failed. Please try Print Selected Results and choose Save as PDF.")}},700);}}\n\nexport default function AdminResultsPage(){');
a = a.replace('th,td{border:1px solid #9faea5;padding:7px 6px;text-align:center;font-size:9.5px}', 'th,td{border:1px solid #9faea5;padding:9px 8px;text-align:center;font-size:11px;line-height:1.35}');
a = a.replace('th{background:#075c2d;color:white;text-transform:uppercase;font-size:8px}', 'th{background:#075c2d;color:white;text-transform:uppercase;font-size:9px}');
a = a.replace('.title{width:max-content;margin:12px auto;border:2px solid #075c2d;padding:6px 24px;color:#075c2d;font-weight:900;letter-spacing:.15em;font-size:17px}', '.title{width:max-content;margin:12px auto;background:#075c2d;border:2px solid #075c2d;padding:7px 26px;color:#fff;font-weight:900;letter-spacing:.15em;font-size:18px;border-radius:4px}');
a = a.replace('.final{margin-top:11px;border:2px solid #075c2d;background:#edf7f1;text-align:center;padding:8px;color:#075c2d;font-weight:900;font-size:15px}', '.final{margin-top:13px;border:2px solid #075c2d;background:#075c2d;text-align:center;padding:9px;color:#fff;font-weight:900;font-size:16px;border-radius:4px}');
fs.writeFileSync(adminPath, a, 'utf8');
