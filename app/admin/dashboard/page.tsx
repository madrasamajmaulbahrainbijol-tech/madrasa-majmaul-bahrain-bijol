"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "@/lib/supabase";

export default function AdminDashboardPage() {
  const router = useRouter();
  const [loading, setLoading] = useState(true); const [email, setEmail] = useState("");
  const [admissionCount, setAdmissionCount] = useState(0); const [historyCount, setHistoryCount] = useState(0); const [studentCount, setStudentCount] = useState(0); const [enquiryCount, setEnquiryCount] = useState(0); const [teacherCount, setTeacherCount] = useState(0); const [noticeCount, setNoticeCount] = useState(0);
  useEffect(() => { async function load() { const { data: { user } } = await supabase.auth.getUser(); if (!user) { router.replace("/admin"); return; } setEmail(user.email || ""); const [{ count: pending }, { count: history }, { count: students }, { count: enquiries }, { count: teachers }, { count: notices }] = await Promise.all([supabase.from("admissions").select("id", { count: "exact", head: true }).eq("status", "new"),supabase.from("admissions").select("id", { count: "exact", head: true }).or("status.neq.approved,account_status.neq.approved"),supabase.from("admissions").select("id", { count: "exact", head: true }).eq("status", "approved").eq("account_status", "approved"),supabase.from("enquiries").select("id", { count: "exact", head: true }).eq("status", "new"),supabase.from("teachers").select("id", { count: "exact", head: true }),supabase.from("notices").select("id", { count: "exact", head: true })]); setAdmissionCount(pending || 0); setHistoryCount(history || 0); setStudentCount(students || 0); setEnquiryCount(enquiries || 0); setTeacherCount(teachers || 0); setNoticeCount(notices || 0); setLoading(false); } load().catch(() => setLoading(false)); }, [router]);
  async function logout() { await supabase.auth.signOut(); router.replace("/admin"); router.refresh(); }
  if (loading) return <main className="flex min-h-screen items-center justify-center bg-gray-50"><div className="text-center"><div className="mx-auto h-12 w-12 animate-spin rounded-full border-4 border-green-200 border-t-green-700"/><p className="mt-4 font-semibold text-gray-600">Loading admin panel...</p></div></main>;
  const Card = ({ icon, title, text, count, label, color, href }: { icon: string; title: string; text: string; count: number; label: string; color: string; href: string }) => <div className="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-md"><div className={`flex h-12 w-12 items-center justify-center rounded-xl ${color} text-2xl`}>{icon}</div><h3 className="mt-5 text-lg font-bold text-gray-900">{title}</h3><p className="mt-2 min-h-10 text-sm text-gray-500">{text}</p><div className="mt-4"><span className="text-3xl font-black text-gray-900">{count}</span><span className="ml-2 text-xs font-semibold text-gray-400">{label}</span></div><button onClick={() => router.push(href)} className="mt-5 text-sm font-bold text-green-700 hover:text-green-900">Open →</button></div>;
  return <main className="min-h-screen bg-gray-50"><header className="border-b border-gray-200 bg-white shadow-sm"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8"><div className="flex items-center gap-3"><img src="/mmbb-logo.svg" alt="Madrasa Majmaul Bahrain Bijol official logo" className="h-14 w-14 rounded-full bg-white object-contain p-0.5 shadow-sm ring-1 ring-green-100"/><div><p className="text-sm font-semibold text-green-700">Madrasa Majmaul Bahrain Bijol</p><h1 className="mt-1 text-2xl font-bold text-gray-900">Admin Dashboard</h1></div></div><button onClick={logout} className="rounded-xl bg-red-600 px-5 py-2.5 font-bold text-white hover:bg-red-700">Logout</button></div></header><section className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><div className="rounded-2xl bg-gradient-to-r from-green-800 to-green-600 p-6 text-white shadow-lg"><p className="text-sm text-green-100">Welcome back</p><h2 className="mt-1 text-2xl font-bold">Madrasa Administration Panel</h2><p className="mt-2 text-sm text-green-100">Logged in as: {email}</p></div><div className="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"><Card icon="➕" title="Take Admission" text="Fill an admission form directly at the office on behalf of a guardian." count={0} label="New Form" color="bg-emerald-100" href="/admin/admissions/take" /><Card icon="🎓" title="Admissions" text="Only untouched applications waiting for a decision." count={admissionCount} label="Pending" color="bg-green-100" href="/admin/admissions" /><Card icon="🗂️" title="Application History" text="Rejected, reviewed, inactive and other non-current records." count={historyCount} label="Records" color="bg-slate-100" href="/admin/students" /><Card icon="👨‍🎓" title="Students" text="Only currently approved and active students." count={studentCount} label="Active" color="bg-purple-100" href="/admin/students" /><Card icon="📩" title="Enquiries" text="New website enquiries waiting for attention." count={enquiryCount} label="New" color="bg-blue-100" href="/admin/enquiries" /><Card icon="👨‍🏫" title="Teachers" text="Manage madrasa teachers and profiles." count={teacherCount} label="Teachers" color="bg-emerald-100" href="/admin/teachers" /><Card icon="📢" title="Notices" text="Create and manage madrasa notices." count={noticeCount} label="Notices" color="bg-amber-100" href="/admin/notices" /><Card icon="🤲" title="Donate Page" text="Update UPI, bank account, QR code and Quran/Hadith content shown publicly." count={0} label="Manage" color="bg-lime-100" href="/admin/donate" /></div><div className="mt-8 rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><h3 className="text-xl font-bold text-gray-900">Management Rules</h3><div className="mt-4 grid gap-4 md:grid-cols-4"><div className="rounded-xl bg-emerald-50 p-4"><p className="font-black text-emerald-800">Take Admission</p><p className="mt-1 text-sm text-emerald-900">Office staff can enter a guardian's form directly. It still follows the normal approval workflow.</p></div><div className="rounded-xl bg-green-50 p-4"><p className="font-black text-green-800">Admissions</p><p className="mt-1 text-sm text-green-900">Only new applications appear here until an admin takes a decision.</p></div><div className="rounded-xl bg-purple-50 p-4"><p className="font-black text-purple-800">Students</p><p className="mt-1 text-sm text-purple-900">Only approved + active accounts count as current students.</p></div><div className="rounded-xl bg-lime-50 p-4"><p className="font-black text-lime-800">Donate Page</p><p className="mt-1 text-sm text-lime-900">The public donation page is controlled from the Donate Page card.</p></div></div></div></section></main>;
}
