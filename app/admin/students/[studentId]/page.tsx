"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { useParams } from "next/navigation";
import { FiActivity, FiArrowLeft, FiAward, FiCalendar, FiCheckCircle, FiCreditCard, FiDownload, FiEdit3, FiFileText, FiHome, FiMapPin, FiMessageCircle, FiPhone, FiPlus, FiPrinter, FiRefreshCw, FiSave, FiShield, FiTrash2, FiUser, FiX } from "react-icons/fi";
import { supabase } from "../../../lib/supabase/client";

type Admission = {
  [key: string]: any;
  id: string; student_id: string | null; student_name: string; guardian_name: string | null; mobile: string | null; email: string | null; course: string | null; date_of_birth: string | null; message: string | null; status: string; created_at: string; approved_at?: string | null; admission_year?: number | null; father_name?: string | null; mother_name?: string | null; guardian_relation?: string | null; contact_number_2?: string | null; alternate_mobile?: string | null; address?: string | null; full_address?: string | null; village_city?: string | null; district?: string | null; state?: string | null; pin_code?: string | null; previous_education?: string | null; occupation?: string | null; post_office?: string | null; country?: string | null; student_photo_url?: string | null; student_document_type?: string | null; student_document_url?: string | null; certificate_url?: string | null; certificate_type?: string | null;
};
type Attendance = { id:string; attendance_date:string; status:"present"|"absent"|"leave"; remarks:string|null };
type ExamMark = { id:string; subject:string; max_marks:number; obtained_marks:number; grade:string|null; result_status:"Pass"|"Fail"; remarks:string|null };
type Exam = { id:string; exam_type:"annual"|"common_test"; exam_name:string; exam_date:string|null; session:string|null; remarks:string|null; created_at:string; student_exam_marks:ExamMark[] };
type FeeSetting = { id:string; monthly_fee:number; effective_from:string; active:boolean };
type FeeLedger = { id:string; fee_month:string; amount_due:number; amount_paid:number; payment_date:string|null; payment_method:string|null; receipt_number:string|null; remarks:string|null };
type ResultRow = { subject:string; max_marks:string; obtained_marks:string; remarks:string };

const emptyResultRow=():ResultRow=>({subject:"",max_marks:"100",obtained_marks:"",remarks:""});
const formatDate=(v:string|null|undefined)=>{if(!v)return"—";const d=new Date(v);return Number.isNaN(d.getTime())?"—":d.toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"})};
const formatMonth=(v:string)=>new Date(`${v}T00:00:00`).toLocaleDateString("en-IN",{month:"long",year:"numeric"});
const money=(v:number|null|undefined)=>`₹${Number(v||0).toLocaleString("en-IN",{maximumFractionDigits:0})}`;
function parseMessage(message:string|null){const out:Record<string,string>={};for(const line of String(message||"").split("\n")){const i=line.indexOf(":");if(i>0)out[line.slice(0,i).trim()]=line.slice(i+1).trim()}return out}
function gradeForPercentage(p:number){if(p>=90)return"A+";if(p>=80)return"A";if(p>=70)return"B+";if(p>=60)return"B";if(p>=50)return"C";if(p>=40)return"D";return"F"}
function safeText(v:unknown){return String(v??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;")}
function printWindow(title:string,body:string){const w=window.open("","_blank","width=1050,height=900");if(!w)return;w.document.write(`<!doctype html><html><head><title>${safeText(title)}</title><meta name="viewport" content="width=device-width,initial-scale=1"><style>*{box-sizing:border-box}body{margin:0;background:#f3f7f4;color:#102018;font-family:Arial,sans-serif;padding:24px}.sheet{max-width:950px;margin:auto;background:#fff;border:1px solid #d7e5dc;border-radius:20px;padding:30px}.brand{border-bottom:3px solid #08783b;padding-bottom:18px;margin-bottom:22px}.brand h1{margin:0;color:#075c2d;font-size:27px}.brand p{margin:6px 0;color:#64748b}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.box{border:1px solid #dfe8e2;border-radius:10px;padding:12px}.label{font-size:10px;text-transform:uppercase;letter-spacing:.08em;color:#64748b;font-weight:800}.value{font-size:14px;font-weight:800;margin-top:5px}.table{width:100%;border-collapse:collapse;margin-top:18px}.table th,.table td{border:1px solid #dce5df;padding:9px;font-size:12px;text-align:left}.table th{background:#edf8f1;color:#075c2d}.summary{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:16px}.summary .box{text-align:center}.sign{display:grid;grid-template-columns:1fr 1fr;gap:100px;margin-top:70px}.sign div{border-top:1px solid #667085;text-align:center;padding-top:7px;font-size:12px}.idcard{max-width:760px;margin:auto;border:1px solid #b8d9c4;border-radius:24px;overflow:hidden}.idtop{background:linear-gradient(135deg,#043b20,#0b9147);color:#fff;padding:25px}.idtop h1{margin:0;font-size:25px}.idtop p{margin:6px 0;color:#d7f4e0}.idbody{padding:24px;display:grid;grid-template-columns:155px 1fr;gap:26px}.photo{width:155px;height:185px;object-fit:cover;border-radius:14px;border:4px solid #d8f0e0}.ph{width:155px;height:185px;border-radius:14px;background:#eaf7ee;color:#08783b;display:flex;align-items:center;justify-content:center;font-size:48px;font-weight:900}.idinfo h2{margin:0;font-size:27px}.idinfo p{margin:8px 0;color:#475569}.idrow{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:16px}.idrow div{border-bottom:1px solid #dce8e0;padding-bottom:7px}.idfoot{background:#eef8f2;padding:14px 24px;display:flex;justify-content:space-between;font-size:11px;color:#52665b}@media(max-width:650px){body{padding:10px}.sheet{padding:18px}.grid,.summary,.sign,.idbody,.idrow{grid-template-columns:1fr}.ph,.photo{margin:auto}.idbody{display:block}}@media print{body{background:#fff;padding:0}.sheet{border:0;max-width:none;padding:0}}</style></head><body><div class="sheet">${body}</div><script>window.onload=function(){setTimeout(function(){window.print()},250)};<\/script></body></html>`);w.document.close()}

export default function StudentProfilePage(){
  const params=useParams<{studentId:string}>();const routeId=decodeURIComponent(params.studentId||"");
  const [student,setStudent]=useState<Admission|null>(null),[attendance,setAttendance]=useState<Attendance[]>([]),[exams,setExams]=useState<Exam[]>([]),[feeSettings,setFeeSettings]=useState<FeeSetting[]>([]),[fees,setFees]=useState<FeeLedger[]>([]);
  const [loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[notice,setNotice]=useState("");
  const [photoUrl,setPhotoUrl]=useState(""),[documentUrl,setDocumentUrl]=useState(""),[certificateUrl,setCertificateUrl]=useState("");
  const [editing,setEditing]=useState(false),[activeResultType,setActiveResultType]=useState<"annual"|"common_test">("annual"),[showResultForm,setShowResultForm]=useState(false),[showAttendanceForm,setShowAttendanceForm]=useState(false),[showFeeForm,setShowFeeForm]=useState(false);
  const [editData,setEditData]=useState({full_name:"",date_of_birth:"",father_name:"",mother_name:"",guardian_name:"",guardian_relation:"",phone:"",alternate_phone:"",occupation:"",address:"",village:"",post_office:"",district:"",state:"",pincode:"",country:"",previous_education:"",course:""});
  const [attendanceForm,setAttendanceForm]=useState({attendance_date:new Date().toISOString().slice(0,10),status:"present" as Attendance["status"],remarks:""});
  const [examForm,setExamForm]=useState({exam_name:"",exam_date:new Date().toISOString().slice(0,10),session:String(new Date().getFullYear()),remarks:""}),[resultRows,setResultRows]=useState<ResultRow[]>([emptyResultRow()]);
  const [monthlyFee,setMonthlyFee]=useState(""),[feeEffectiveFrom,setFeeEffectiveFrom]=useState(new Date().toISOString().slice(0,10));
  const [feeForm,setFeeForm]=useState({feeId:"",amount_paid:"",payment_date:new Date().toISOString().slice(0,10),payment_method:"Cash",remarks:""});
  const messageData=useMemo(()=>parseMessage(student?.message||""),[student?.message]);

  const loadAll=async()=>{setLoading(true);setError("");try{
    let r=await supabase.from("admissions").select("*").eq("student_id",routeId).maybeSingle();if(!r.data&&!r.error)r=await supabase.from("admissions").select("*").eq("id",routeId).maybeSingle();if(r.error)throw r.error;if(!r.data)throw new Error("Student record not found.");
    const a=r.data as Admission;setStudent(a);const p=parseMessage(a.message);
    setEditData({full_name:a.student_name||"",date_of_birth:a.date_of_birth||"",father_name:a.father_name||p["Father's Name"]||"",mother_name:a.mother_name||p["Mother's Name"]||"",guardian_name:a.guardian_name||p["Guardian Name"]||"",guardian_relation:a.guardian_relation||p["Guardian Relationship"]||"",phone:a.mobile||p["Mobile Number 1"]||"",alternate_phone:a.contact_number_2||a.alternate_mobile||p["Mobile Number 2"]||"",occupation:a.occupation||p["Parent Occupation"]||"",address:a.full_address||a.address||p["Address"]||"",village:a.village_city||p["Village / Town"]||"",post_office:a.post_office||p["Post Office"]||"",district:a.district||p["District"]||"",state:a.state||p["State"]||"",pincode:a.pin_code||p["PIN Code"]||"",country:a.country||p["Country"]||"India",previous_education:a.previous_education||p["Previous Education"]||"",course:a.course||""});
    const [aa,ee,fs,ff]=await Promise.all([
      supabase.from("student_attendance").select("id,attendance_date,status,remarks").eq("admission_id",a.id).order("attendance_date",{ascending:false}).limit(100),
      supabase.from("student_exams").select("id,exam_type,exam_name,exam_date,session,remarks,created_at,student_exam_marks(id,subject,max_marks,obtained_marks,grade,result_status,remarks)").eq("admission_id",a.id).order("exam_date",{ascending:false}),
      supabase.from("student_fee_settings").select("id,monthly_fee,effective_from,active").eq("admission_id",a.id).order("effective_from",{ascending:false}),
      supabase.from("student_fee_ledger").select("id,fee_month,amount_due,amount_paid,payment_date,payment_method,receipt_number,remarks").eq("admission_id",a.id).order("fee_month",{ascending:false})
    ]);if(aa.error)throw aa.error;if(ee.error)throw ee.error;if(fs.error)throw fs.error;if(ff.error)throw ff.error;
    setAttendance((aa.data||[]) as Attendance[]);setExams((ee.data||[]) as Exam[]);setFeeSettings((fs.data||[]) as FeeSetting[]);setFees((ff.data||[]) as FeeLedger[]);
    if((fs.data||[]).some((x:any)=>x.active)){const {error:rpcError}=await supabase.rpc("ensure_student_fee_ledger",{p_admission_id:a.id});if(!rpcError){const {data:latestFees}=await supabase.from("student_fee_ledger").select("id,fee_month,amount_due,amount_paid,payment_date,payment_method,receipt_number,remarks").eq("admission_id",a.id).order("fee_month",{ascending:false});setFees((latestFees||[]) as FeeLedger[])} }
    const photoPath=a.student_photo_url||p["Student Photo"]||"",docPath=a.student_document_url||p["Identity Proof"]||"",certPath=a.certificate_url||p["Certificate"]||"";
    if(photoPath){const {data}=await supabase.storage.from("admission-documents").createSignedUrl(photoPath,3600);setPhotoUrl(data?.signedUrl||"")}else setPhotoUrl("");
    if(docPath){const {data}=await supabase.storage.from("admission-documents").createSignedUrl(docPath,3600);setDocumentUrl(data?.signedUrl||"")}else setDocumentUrl("");
    if(certPath){const {data}=await supabase.storage.from("admission-documents").createSignedUrl(certPath,3600);setCertificateUrl(data?.signedUrl||"")}else setCertificateUrl("");
  }catch(e:any){setError(e?.message||"Unable to load student profile.")}finally{setLoading(false)}};
  useEffect(()=>{loadAll()},[routeId]);

  const currentFee=feeSettings.find(x=>x.active)?.monthly_fee||0,paidTotal=fees.reduce((s,x)=>s+Number(x.amount_paid||0),0),dueTotal=fees.reduce((s,x)=>s+Math.max(0,Number(x.amount_due||0)-Number(x.amount_paid||0)),0),totalBilled=fees.reduce((s,x)=>s+Number(x.amount_due||0),0);
  const present=attendance.filter(x=>x.status==="present").length,absent=attendance.filter(x=>x.status==="absent").length,leave=attendance.filter(x=>x.status==="leave").length,totalAttendance=present+absent+leave,attendancePct=totalAttendance?Math.round(present/totalAttendance*100):0;
  const resultExams=exams.filter(x=>x.exam_type===activeResultType);const initials=(student?.student_name||"S").split(/\s+/).map(x=>x[0]).slice(0,2).join("").toUpperCase();
  const go=(id:string)=>document.getElementById(id)?.scrollIntoView({behavior:"smooth",block:"start"});
  const updateEdit=(k:string,v:string)=>setEditData(x=>({...x,[k]:v}));

  const saveProfile=async()=>{if(!student){return}setSaving(true);setError("");try{const p=parseMessage(student.message);const msg=[`Application Number: ${p["Application Number"]||""}`,`Father's Name: ${editData.father_name||"Not provided"}`,`Mother's Name: ${editData.mother_name||"Not provided"}`,`Guardian Name: ${editData.guardian_name||"Not provided"}`,`Guardian Relationship: ${editData.guardian_relation||"Not provided"}`,`Mobile Number 1: ${editData.phone||"Not provided"}`,`Mobile Number 2: ${editData.alternate_phone||"Not provided"}`,`Parent Occupation: ${editData.occupation||"Not provided"}`,`Previous Education: ${editData.previous_education||"Not provided"}`,`Address: ${editData.address||"Not provided"}`,`Village / Town: ${editData.village||"Not provided"}`,`Post Office: ${editData.post_office||"Not provided"}`,`District: ${editData.district||"Not provided"}`,`State: ${editData.state||"Not provided"}`,`PIN Code: ${editData.pincode||"Not provided"}`,`Country: ${editData.country||"India"}`].join("\n");const{error:e}=await supabase.from("admissions").update({student_name:editData.full_name,date_of_birth:editData.date_of_birth||null,father_name:editData.father_name||null,mother_name:editData.mother_name||null,guardian_name:editData.guardian_name||null,guardian_relation:editData.guardian_relation||null,mobile:editData.phone||null,contact_number_2:editData.alternate_phone||null,occupation:editData.occupation||null,full_address:editData.address||null,address:editData.address||null,village_city:editData.village||null,post_office:editData.post_office||null,district:editData.district||null,state:editData.state||null,pin_code:editData.pincode||null,country:editData.country||null,previous_education:editData.previous_education||null,course:editData.course||null,message:msg}).eq("id",student.id);if(e)throw e;setEditing(false);setNotice("Student profile updated.");await loadAll()}catch(e:any){setError(e?.message||"Unable to update profile")}finally{setSaving(false)}};

  const saveAttendance=async()=>{if(!student)return;setSaving(true);setError("");try{const{error:e}=await supabase.from("student_attendance").upsert({admission_id:student.id,attendance_date:attendanceForm.attendance_date,status:attendanceForm.status,remarks:attendanceForm.remarks||null},{onConflict:"admission_id,attendance_date"});if(e)throw e;setShowAttendanceForm(false);setNotice("Attendance saved.");await loadAll()}catch(e:any){setError(e?.message||"Unable to save attendance")}finally{setSaving(false)}};

  const saveExam=async()=>{if(!student)return;setSaving(true);setError("");setNotice("");try{const rows=resultRows.map(r=>({subject:r.subject.trim(),max_marks:Number(r.max_marks),obtained_marks:Number(r.obtained_marks||0),remarks:r.remarks.trim()})).filter(r=>r.subject&&Number.isFinite(r.max_marks)&&r.max_marks>0);if(!examForm.exam_name.trim())throw new Error("Exam name is required.");if(!rows.length)throw new Error("Add at least one subject with maximum marks.");for(const r of rows)if(r.obtained_marks<0||r.obtained_marks>r.max_marks)throw new Error(`Obtained marks for ${r.subject} must be between 0 and ${r.max_marks}.`);const{error:e}=await supabase.rpc("save_student_exam",{p_admission_id:student.id,p_exam_type:activeResultType,p_exam_name:examForm.exam_name.trim(),p_exam_date:examForm.exam_date||null,p_session:examForm.session||null,p_remarks:examForm.remarks.trim()||null,p_marks:rows});if(e)throw e;setShowResultForm(false);setResultRows([emptyResultRow()]);setExamForm({exam_name:"",exam_date:new Date().toISOString().slice(0,10),session:String(new Date().getFullYear()),remarks:""});setNotice("Complete result saved successfully.");await loadAll()}catch(e:any){setError(e?.message||"Unable to save result")}finally{setSaving(false)}};

  const deleteExam=async(id:string)=>{if(!confirm("Delete this complete result?"))return;const{error:e}=await supabase.from("student_exams").delete().eq("id",id);if(e)setError(e.message);else{setNotice("Result deleted.");await loadAll()}};

  const saveMonthlyFee=async()=>{if(!student)return;const amount=Number(monthlyFee);if(!Number.isFinite(amount)||amount<0){setError("Enter a valid monthly fee.");return}setSaving(true);setError("");try{const{error:u}=await supabase.from("student_fee_settings").update({active:false}).eq("admission_id",student.id);if(u)throw u;const{error:e}=await supabase.from("student_fee_settings").insert({admission_id:student.id,monthly_fee:amount,effective_from:feeEffectiveFrom,active:true});if(e)throw e;const{error:rpc}=await supabase.rpc("ensure_student_fee_ledger",{p_admission_id:student.id});if(rpc)throw rpc;setMonthlyFee("");setShowFeeForm(false);setNotice(`Monthly fee ${money(amount)} set. Monthly ledger has been updated.`);await loadAll()}catch(e:any){setError(e?.message||"Unable to set monthly fee")}finally{setSaving(false)}};

  const recordFeePayment=async(fee:FeeLedger)=>{const outstanding=Math.max(0,Number(fee.amount_due)-Number(fee.amount_paid));const amount=Number(feeForm.amount_paid);if(!Number.isFinite(amount)||amount<=0){setError("Enter a valid payment amount.");return}if(amount>outstanding){setError(`Payment cannot exceed this month's outstanding ${money(outstanding)}.`);return}setSaving(true);setError("");try{const receipt=fee.receipt_number||`MMBB-FEE-${new Date().getFullYear()}-${Date.now().toString().slice(-7)}`;const{error:e}=await supabase.from("student_fee_ledger").update({amount_paid:Number(fee.amount_paid)+amount,payment_date:feeForm.payment_date,payment_method:feeForm.payment_method,receipt_number:receipt,remarks:feeForm.remarks||fee.remarks||null}).eq("id",fee.id);if(e)throw e;setFeeForm({feeId:"",amount_paid:"",payment_date:new Date().toISOString().slice(0,10),payment_method:"Cash",remarks:""});setNotice(`${money(amount)} payment recorded for ${formatMonth(fee.fee_month)}.`);await loadAll()}catch(e:any){setError(e?.message||"Unable to record payment")}finally{setSaving(false)}};

  const printResult=(exam:Exam)=>{if(!student)return;const max=exam.student_exam_marks.reduce((s,m)=>s+Number(m.max_marks),0),got=exam.student_exam_marks.reduce((s,m)=>s+Number(m.obtained_marks),0),pct=max?got/max*100:0,pass=exam.student_exam_marks.every(m=>m.result_status==="Pass");const rows=exam.student_exam_marks.map(m=>`<tr><td>${safeText(m.subject)}</td><td>${m.max_marks}</td><td>${m.obtained_marks}</td><td>${(Number(m.obtained_marks)/Number(m.max_marks)*100).toFixed(1)}%</td><td>${safeText(m.grade||gradeForPercentage(Number(m.obtained_marks)/Number(m.max_marks)*100))}</td><td>${m.result_status}</td><td>${safeText(m.remarks||"—")}</td></tr>`).join("");printWindow(`${exam.exam_name} Result`,`<div class="brand"><h1>Madrasa Majmaul Bahrain Bijol</h1><p>Official Student Result</p></div><div class="grid"><div class="box"><div class="label">Student Name</div><div class="value">${safeText(student.student_name)}</div></div><div class="box"><div class="label">Student ID</div><div class="value">${safeText(student.student_id)}</div></div><div class="box"><div class="label">Examination</div><div class="value">${exam.exam_type==="annual"?"Annual Examination":"Common Test"}</div></div><div class="box"><div class="label">Session</div><div class="value">${safeText(exam.session||"—")}</div></div><div class="box"><div class="label">Exam Date</div><div class="value">${formatDate(exam.exam_date)}</div></div><div class="box"><div class="label">Final Result</div><div class="value">${pass?"PASS":"FAIL"}</div></div></div><table class="table"><thead><tr><th>Subject</th><th>Max</th><th>Obtained</th><th>%</th><th>Grade</th><th>Result</th><th>Remarks</th></tr></thead><tbody>${rows}</tbody></table><div class="summary"><div class="box"><div class="label">Total Marks</div><div class="value">${max}</div></div><div class="box"><div class="label">Obtained</div><div class="value">${got}</div></div><div class="box"><div class="label">Percentage</div><div class="value">${pct.toFixed(2)}%</div></div><div class="box"><div class="label">Grade</div><div class="value">${gradeForPercentage(pct)}</div></div></div><p><strong>Remarks:</strong> ${safeText(exam.remarks||"—")}</p><div class="sign"><div>Class / Subject Teacher</div><div>Principal / Authorized Signatory</div></div>`)};

  const printReceipt=(fee:FeeLedger)=>{if(!student)return;const paid=Number(fee.amount_paid),due=Number(fee.amount_due),balance=Math.max(0,due-paid);printWindow(`Fee Receipt ${fee.receipt_number||""}`,`<div class="brand"><h1>Madrasa Majmaul Bahrain Bijol</h1><p>Official Monthly Fee Receipt</p></div><div class="grid"><div class="box"><div class="label">Receipt Number</div><div class="value">${safeText(fee.receipt_number||"—")}</div></div><div class="box"><div class="label">Payment Date</div><div class="value">${formatDate(fee.payment_date)}</div></div><div class="box"><div class="label">Student Name</div><div class="value">${safeText(student.student_name)}</div></div><div class="box"><div class="label">Student ID</div><div class="value">${safeText(student.student_id)}</div></div><div class="box"><div class="label">Fee Month</div><div class="value">${formatMonth(fee.fee_month)}</div></div><div class="box"><div class="label">Payment Method</div><div class="value">${safeText(fee.payment_method||"—")}</div></div></div><table class="table"><thead><tr><th>Description</th><th>Amount</th></tr></thead><tbody><tr><td>Monthly Madrasa Fee</td><td>${money(paid)}</td></tr></tbody></table><div class="summary"><div class="box"><div class="label">Monthly Due</div><div class="value">${money(due)}</div></div><div class="box"><div class="label">Paid</div><div class="value">${money(paid)}</div></div><div class="box"><div class="label">Balance</div><div class="value">${money(balance)}</div></div><div class="box"><div class="label">Status</div><div class="value">${paid>=due?"PAID":"PARTIAL"}</div></div></div><div class="sign"><div>Received By</div><div>Authorized Signatory</div></div>`)};

  const printIdCard=()=>{if(!student)return;const photo=photoUrl?`<img class="photo" src="${safeText(photoUrl)}" alt="Student Photo">`:`<div class="ph">${safeText(initials)}</div>`;printWindow(`Student ID Card - ${student.student_name}`,`<div class="idcard"><div class="idtop"><h1>Madrasa Majmaul Bahrain Bijol</h1><p>A Centre of Islamic Education</p></div><div class="idbody">${photo}<div class="idinfo"><h2>${safeText(student.student_name)}</h2><p><strong>Student ID:</strong> ${safeText(student.student_id||"—")}</p><div class="idrow"><div><small>Date of Birth</small><br><strong>${formatDate(student.date_of_birth)}</strong></div><div><small>Course</small><br><strong>${safeText(student.course||"—")}</strong></div><div><small>Father / Guardian</small><br><strong>${safeText(student.father_name||student.guardian_name||messageData["Guardian Name"]||"—")}</strong></div><div><small>Guardian Mobile</small><br><strong>${safeText(student.mobile||"—")}</strong></div><div><small>Village / Town</small><br><strong>${safeText(student.village_city||messageData["Village / Town"]||"—")}</strong></div><div><small>District / State</small><br><strong>${safeText([student.district||messageData["District"],student.state||messageData["State"]].filter(Boolean).join(" / ")||"—")}</strong></div></div></div></div><div class="idfoot"><span>Issued by Madrasa Administration</span><span>Admission Year: ${student.admission_year||new Date(student.created_at).getFullYear()}</span></div></div>`)};

  if(loading)return <main className="min-h-screen bg-slate-50 flex items-center justify-center"><div className="text-center"><FiRefreshCw className="mx-auto animate-spin text-4xl text-green-700"/><p className="mt-4 font-bold text-slate-700">Loading student profile...</p></div></main>;
  if(!student)return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto mt-20 max-w-xl rounded-3xl bg-white p-10 text-center shadow-xl"><FiUser className="mx-auto text-5xl text-red-500"/><h1 className="mt-4 text-2xl font-extrabold">Student not found</h1><p className="mt-2 text-slate-500">{error||"The requested student record could not be found."}</p><Link href="/admin/students" className="mt-6 inline-flex rounded-xl bg-green-700 px-5 py-3 font-bold text-white">Back to Students</Link></div></main>;

  const nav=[{id:"overview",label:"Overview",icon:<FiHome/>},{id:"attendance",label:"Attendance",icon:<FiCheckCircle/>},{id:"results",label:"Results",icon:<FiAward/>},{id:"fees",label:"Fees",icon:<FiCreditCard/>},{id:"documents",label:"Documents",icon:<FiFileText/>},{id:"id-card",label:"ID Card",icon:<FiCreditCard/>},{id:"activity",label:"Activity",icon:<FiActivity/>}];
  return <main className="min-h-screen bg-[#f4f7f5] text-slate-900">
    <header className="sticky top-0 z-50 border-b border-slate-200 bg-white/95 backdrop-blur-xl shadow-sm"><div className="mx-auto max-w-[1550px] px-3 sm:px-5 lg:px-8"><div className="flex items-center justify-between gap-3 py-2.5"><Link href="/admin/students" className="inline-flex shrink-0 items-center gap-2 rounded-xl px-2.5 py-2 text-sm font-black text-slate-600 hover:bg-slate-100"><FiArrowLeft/><span className="hidden sm:inline">Students</span></Link><div className="hidden text-center md:block"><p className="text-[9px] font-black uppercase tracking-[0.3em] text-green-700">Madrasa Majmaul Bahrain Bijol</p><p className="text-sm font-black">Student Profile</p></div><div className="flex items-center gap-2"><button onClick={loadAll} title="Refresh" className="rounded-xl border border-slate-200 p-2.5 text-slate-600 hover:bg-slate-50"><FiRefreshCw/></button><button onClick={()=>setEditing(v=>!v)} className="inline-flex items-center gap-2 rounded-xl bg-slate-900 px-3 py-2.5 text-sm font-black text-white"><FiEdit3/><span className="hidden sm:inline">Edit</span></button></div></div><nav className="flex gap-1 overflow-x-auto pb-2">{nav.map(n=><button key={n.id} onClick={()=>go(n.id)} className="inline-flex shrink-0 items-center gap-1.5 rounded-xl border border-transparent px-3 py-2 text-xs font-black text-slate-600 hover:border-green-100 hover:bg-green-50 hover:text-green-700 sm:text-sm">{n.icon}{n.label}</button>)}</nav></div></header>
    <section className="mx-auto max-w-[1550px] px-3 py-5 sm:px-5 lg:px-8 lg:py-7">
      {(error||notice)&&<div className={`mb-5 flex items-start justify-between gap-4 rounded-2xl border px-5 py-4 font-bold ${error?"border-red-200 bg-red-50 text-red-700":"border-green-200 bg-green-50 text-green-800"}`}><span>{error||notice}</span><button onClick={()=>{setError("");setNotice("")}}><FiX/></button></div>}
      <section id="overview" className="scroll-mt-36 rounded-3xl bg-gradient-to-br from-green-950 via-green-800 to-green-600 p-5 text-white shadow-xl sm:p-7"><div className="flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-center gap-5"><div className="h-24 w-24 shrink-0 overflow-hidden rounded-2xl border-4 border-white/20 bg-white/10">{photoUrl?<img src={photoUrl} alt={student.student_name} className="h-full w-full object-cover"/>:<div className="flex h-full items-center justify-center text-3xl font-black">{initials}</div>}</div><div><p className="text-xs font-black uppercase tracking-[.25em] text-green-200">Student Profile</p><h1 className="mt-1 text-3xl font-black sm:text-4xl">{student.student_name}</h1><p className="mt-2 font-bold text-green-100">{student.student_id||"Student ID not generated"} • {student.course||"Course not assigned"}</p><p className="mt-1 text-sm text-green-200">Admission: {formatDate(student.approved_at||student.created_at)}</p></div></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-green-200">Attendance</p><p className="mt-1 text-2xl font-black">{attendancePct}%</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-green-200">Monthly Fee</p><p className="mt-1 text-2xl font-black">{money(currentFee)}</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-green-200">Total Paid</p><p className="mt-1 text-2xl font-black">{money(paidTotal)}</p></div><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs text-green-200">Overdue</p><p className="mt-1 text-2xl font-black">{money(dueTotal)}</p></div></div></div></section>

      {editing&&<section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Edit</p><h2 className="text-2xl font-black">Student Information</h2></div><button onClick={()=>setEditing(false)}><FiX/></button></div><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{([['full_name','Full Name'],['date_of_birth','Date of Birth'],['father_name','Father Name'],['mother_name','Mother Name'],['guardian_name','Guardian Name'],['guardian_relation','Guardian Relation'],['phone','Mobile'],['alternate_phone','Alternate Mobile'],['occupation','Occupation'],['address','Address'],['village','Village / Town'],['post_office','Post Office'],['district','District'],['state','State'],['pincode','PIN Code'],['country','Country'],['previous_education','Previous Education'],['course','Course']] as [string,string][]).map(([k,l])=><label key={k} className="block"><span className="mb-1.5 block text-sm font-bold text-slate-700">{l}</span><input type={k==='date_of_birth'?'date':'text'} value={(editData as any)[k]} onChange={e=>updateEdit(k,e.target.value)} className="w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-green-600 focus:ring-4 focus:ring-green-100"/></label>)}</div><button onClick={saveProfile} disabled={saving} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-green-700 px-6 py-3 font-black text-white disabled:opacity-50"><FiSave/> {saving?"Saving...":"Save Profile"}</button></section>}

      <section className="mt-7 grid gap-6 lg:grid-cols-3"><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><div className="mb-5 flex items-center gap-3"><FiUser className="text-xl text-green-700"/><h2 className="text-xl font-black">Basic Information</h2></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[["Full Name",student.student_name],["Date of Birth",formatDate(student.date_of_birth)],["Gender",student.gender||messageData["Gender"]||"—"],["Father's Name",student.father_name||messageData["Father's Name"]||"—"],["Mother's Name",student.mother_name||messageData["Mother's Name"]||"—"],["Course",student.course||"—"],["Previous Education",student.previous_education||messageData["Previous Education"]||"—"],["Mobile",student.mobile||"—"],["Email",student.email||"—"]].map(([l,v])=><div key={l} className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-black uppercase tracking-wider text-slate-400">{l}</p><p className="mt-1 break-words font-bold text-slate-800">{v||"—"}</p></div>)}</div></div><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="mb-5 flex items-center gap-3"><FiMapPin className="text-xl text-green-700"/><h2 className="text-xl font-black">Guardian & Address</h2></div><div className="space-y-4">{[["Guardian",student.guardian_name||messageData["Guardian Name"]||"—"],["Relation",student.guardian_relation||messageData["Guardian Relationship"]||"—"],["Mobile",student.mobile||"—"],["Alternate",student.contact_number_2||student.alternate_mobile||messageData["Mobile Number 2"]||"—"],["Occupation",student.occupation||messageData["Parent Occupation"]||"—"],["Address",student.full_address||student.address||messageData["Address"]||"—"],["Village / Town",student.village_city||messageData["Village / Town"]||"—"],["District / State",[student.district,student.state].filter(Boolean).join(" / ")||"—"],["PIN / Country",[student.pin_code,student.country].filter(Boolean).join(" / ")||"—"]].map(([l,v])=><div key={l}><p className="text-xs font-black uppercase tracking-wider text-slate-400">{l}</p><p className="mt-1 font-bold text-slate-800">{v||"—"}</p></div>)}</div></div></section>

      <section id="attendance" className="mt-7 scroll-mt-36 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Attendance</p><h2 className="text-2xl font-black">Attendance Record</h2></div><button onClick={()=>setShowAttendanceForm(v=>!v)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-green-700 px-5 py-3 font-black text-white"><FiPlus/> Mark Attendance</button></div><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="rounded-2xl bg-green-50 p-4"><p className="text-xs font-bold text-green-700">Present</p><p className="text-2xl font-black">{present}</p></div><div className="rounded-2xl bg-red-50 p-4"><p className="text-xs font-bold text-red-700">Absent</p><p className="text-2xl font-black">{absent}</p></div><div className="rounded-2xl bg-amber-50 p-4"><p className="text-xs font-bold text-amber-700">Leave</p><p className="text-2xl font-black">{leave}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-600">Percentage</p><p className="text-2xl font-black">{attendancePct}%</p></div></div>{showAttendanceForm&&<div className="mt-5 rounded-2xl bg-slate-50 p-5"><div className="grid gap-4 md:grid-cols-3"><label><span className="mb-1 block text-sm font-bold">Date</span><input type="date" value={attendanceForm.attendance_date} onChange={e=>setAttendanceForm(x=>({...x,attendance_date:e.target.value}))} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label><label><span className="mb-1 block text-sm font-bold">Status</span><select value={attendanceForm.status} onChange={e=>setAttendanceForm(x=>({...x,status:e.target.value as Attendance["status"]}))} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"><option value="present">Present</option><option value="absent">Absent</option><option value="leave">Leave</option></select></label><label><span className="mb-1 block text-sm font-bold">Remarks</span><input value={attendanceForm.remarks} onChange={e=>setAttendanceForm(x=>({...x,remarks:e.target.value}))} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label></div><button onClick={saveAttendance} disabled={saving} className="mt-4 rounded-xl bg-slate-900 px-5 py-2.5 font-black text-white">{saving?"Saving...":"Save Attendance"}</button></div>}<div className="mt-5 overflow-x-auto"><table className="w-full min-w-[650px] text-sm"><thead><tr className="border-b text-left text-xs uppercase tracking-wider text-slate-400"><th className="p-3">Date</th><th className="p-3">Status</th><th className="p-3">Remarks</th></tr></thead><tbody>{attendance.length?attendance.map(a=><tr key={a.id} className="border-b border-slate-100"><td className="p-3 font-bold">{formatDate(a.attendance_date)}</td><td className="p-3"><span className={`rounded-full px-3 py-1 text-xs font-black ${a.status==='present'?'bg-green-100 text-green-700':a.status==='absent'?'bg-red-100 text-red-700':'bg-amber-100 text-amber-700'}`}>{a.status}</span></td><td className="p-3 text-slate-500">{a.remarks||"—"}</td></tr>):<tr><td colSpan={3} className="p-8 text-center text-slate-400">No attendance entries yet.</td></tr>}</tbody></table></div></section>

      <section id="results" className="mt-7 scroll-mt-36 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Examination</p><h2 className="text-2xl font-black">Results</h2></div><div className="flex flex-wrap gap-2"><button onClick={()=>setActiveResultType("annual")} className={`rounded-xl px-4 py-2.5 text-sm font-black ${activeResultType==='annual'?'bg-green-700 text-white':'bg-slate-100 text-slate-600'}`}>Annual Examination</button><button onClick={()=>setActiveResultType("common_test")} className={`rounded-xl px-4 py-2.5 text-sm font-black ${activeResultType==='common_test'?'bg-green-700 text-white':'bg-slate-100 text-slate-600'}`}>Common Test</button><button onClick={()=>setShowResultForm(v=>!v)} className="inline-flex items-center gap-2 rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-black text-white"><FiPlus/> Add Result</button></div></div>
        {showResultForm&&<div className="mt-5 rounded-2xl bg-slate-50 p-5"><div className="grid gap-4 md:grid-cols-3"><label><span className="mb-1 block text-sm font-bold">Exam Name</span><input value={examForm.exam_name} onChange={e=>setExamForm(x=>({...x,exam_name:e.target.value}))} placeholder={activeResultType==='annual'?'Annual Examination':'Common Test'} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label><label><span className="mb-1 block text-sm font-bold">Exam Date</span><input type="date" value={examForm.exam_date} onChange={e=>setExamForm(x=>({...x,exam_date:e.target.value}))} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label><label><span className="mb-1 block text-sm font-bold">Session</span><input value={examForm.session} onChange={e=>setExamForm(x=>({...x,session:e.target.value}))} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[750px]"><thead><tr className="text-left text-xs uppercase tracking-wider text-slate-400"><th className="p-2">Subject</th><th className="p-2">Maximum Marks</th><th className="p-2">Obtained</th><th className="p-2">Remarks</th><th/></tr></thead><tbody>{resultRows.map((r,i)=><tr key={i}><td className="p-2"><input value={r.subject} onChange={e=>setResultRows(rows=>rows.map((x,n)=>n===i?{...x,subject:e.target.value}:x))} placeholder="Subject" className="w-full rounded-lg border border-slate-200 px-3 py-2"/></td><td className="p-2"><input type="number" min="1" value={r.max_marks} onChange={e=>setResultRows(rows=>rows.map((x,n)=>n===i?{...x,max_marks:e.target.value}:x))} className="w-full rounded-lg border border-slate-200 px-3 py-2"/></td><td className="p-2"><input type="number" min="0" value={r.obtained_marks} onChange={e=>setResultRows(rows=>rows.map((x,n)=>n===i?{...x,obtained_marks:e.target.value}:x))} className="w-full rounded-lg border border-slate-200 px-3 py-2"/></td><td className="p-2"><input value={r.remarks} onChange={e=>setResultRows(rows=>rows.map((x,n)=>n===i?{...x,remarks:e.target.value}:x))} className="w-full rounded-lg border border-slate-200 px-3 py-2"/></td><td className="p-2">{resultRows.length>1&&<button type="button" onClick={()=>setResultRows(rows=>rows.filter((_,n)=>n!==i))} className="rounded-lg p-2 text-red-600"><FiTrash2/></button>}</td></tr>)}</tbody></table></div><div className="mt-3 flex flex-wrap gap-3"><button type="button" onClick={()=>setResultRows(rows=>[...rows,emptyResultRow()])} className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 font-black">+ Add Subject</button><textarea value={examForm.remarks} onChange={e=>setExamForm(x=>({...x,remarks:e.target.value}))} placeholder="Overall result remarks" className="min-h-11 flex-1 rounded-xl border border-slate-200 px-3 py-2.5"/><button type="button" onClick={saveExam} disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-green-700 px-5 py-2.5 font-black text-white disabled:opacity-50"><FiSave/> {saving?"Saving...":"Save Complete Result"}</button></div></div>}
        <div className="mt-5 space-y-5">{resultExams.length?resultExams.map(exam=>{const max=exam.student_exam_marks.reduce((s,m)=>s+Number(m.max_marks),0),got=exam.student_exam_marks.reduce((s,m)=>s+Number(m.obtained_marks),0),pct=max?got/max*100:0,pass=exam.student_exam_marks.length>0&&exam.student_exam_marks.every(m=>m.result_status==='Pass');return <div key={exam.id} className="rounded-2xl border border-slate-200 overflow-hidden"><div className="flex flex-col justify-between gap-4 bg-slate-50 p-5 sm:flex-row sm:items-center"><div><h3 className="text-lg font-black">{exam.exam_name}</h3><p className="mt-1 text-sm text-slate-500">{formatDate(exam.exam_date)} • Session {exam.session||"—"}</p></div><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-3 py-1 text-xs font-black ${pass?'bg-green-100 text-green-700':'bg-red-100 text-red-700'}`}>{pass?'PASS':'FAIL'}</span><span className="rounded-full bg-white px-3 py-1 text-xs font-black">{pct.toFixed(2)}%</span><button onClick={()=>printResult(exam)} className="inline-flex items-center gap-2 rounded-xl bg-green-700 px-4 py-2 text-sm font-black text-white"><FiPrinter/> Print Result</button><button onClick={()=>deleteExam(exam.id)} className="rounded-xl border border-red-200 p-2 text-red-600"><FiTrash2/></button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-sm"><thead><tr className="border-b text-left text-xs uppercase tracking-wider text-slate-400"><th className="p-3">Subject</th><th className="p-3">Max</th><th className="p-3">Obtained</th><th className="p-3">%</th><th className="p-3">Grade</th><th className="p-3">Result</th><th className="p-3">Remarks</th></tr></thead><tbody>{exam.student_exam_marks.map(m=><tr key={m.id} className="border-b border-slate-100"><td className="p-3 font-bold">{m.subject}</td><td className="p-3">{m.max_marks}</td><td className="p-3">{m.obtained_marks}</td><td className="p-3">{(Number(m.obtained_marks)/Number(m.max_marks)*100).toFixed(1)}%</td><td className="p-3 font-black">{m.grade}</td><td className="p-3 font-black">{m.result_status}</td><td className="p-3 text-slate-500">{m.remarks||"—"}</td></tr>)}</tbody></table></div><div className="grid gap-3 border-t bg-white p-4 sm:grid-cols-4"><div><p className="text-xs text-slate-400">Total</p><p className="font-black">{max}</p></div><div><p className="text-xs text-slate-400">Obtained</p><p className="font-black">{got}</p></div><div><p className="text-xs text-slate-400">Percentage</p><p className="font-black">{pct.toFixed(2)}%</p></div><div><p className="text-xs text-slate-400">Overall Grade</p><p className="font-black">{gradeForPercentage(pct)}</p></div></div></div>}) : <div className="rounded-2xl bg-slate-50 p-10 text-center text-slate-400">No {activeResultType==='annual'?'Annual Examination':'Common Test'} results saved yet.</div>}</div>
      </section>

      <section id="fees" className="mt-7 scroll-mt-36 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-center"><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Finance</p><h2 className="text-2xl font-black">Monthly Fee Ledger</h2><p className="mt-1 text-sm text-slate-500">Every month gets its own due, paid and outstanding balance.</p></div><button onClick={()=>setShowFeeForm(v=>!v)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-green-700 px-5 py-3 font-black text-white"><FiPlus/> {currentFee?"Change Monthly Fee":"Set Monthly Fee"}</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-2xl bg-green-50 p-5"><p className="text-xs font-bold text-green-700">Monthly Fee</p><p className="mt-1 text-2xl font-black">{money(currentFee)}</p></div><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs font-bold text-slate-600">Total Billed</p><p className="mt-1 text-2xl font-black">{money(totalBilled)}</p></div><div className="rounded-2xl bg-blue-50 p-5"><p className="text-xs font-bold text-blue-700">Total Paid</p><p className="mt-1 text-2xl font-black">{money(paidTotal)}</p></div><div className="rounded-2xl bg-red-50 p-5"><p className="text-xs font-bold text-red-700">Total Overdue</p><p className="mt-1 text-2xl font-black text-red-700">{money(dueTotal)}</p></div></div>
        {showFeeForm&&<div className="mt-5 rounded-2xl bg-slate-50 p-5"><div className="grid gap-4 md:grid-cols-2"><label><span className="mb-1 block text-sm font-bold">Fixed Monthly Fee</span><input type="number" min="0" value={monthlyFee} onChange={e=>setMonthlyFee(e.target.value)} placeholder="700" className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label><label><span className="mb-1 block text-sm font-bold">Fee Starts From</span><input type="date" value={feeEffectiveFrom} onChange={e=>setFeeEffectiveFrom(e.target.value)} className="w-full rounded-xl border border-slate-200 px-3 py-2.5"/></label></div><button onClick={saveMonthlyFee} disabled={saving} className="mt-4 rounded-xl bg-slate-900 px-5 py-2.5 font-black text-white">{saving?"Saving...":"Save Monthly Fee & Generate Ledger"}</button></div>}
        <div className="mt-5 overflow-x-auto"><table className="w-full min-w-[900px] text-sm"><thead><tr className="border-b bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-400"><th className="p-3">Month</th><th className="p-3">Due</th><th className="p-3">Paid</th><th className="p-3">Outstanding</th><th className="p-3">Payment Date</th><th className="p-3">Method</th><th className="p-3">Receipt</th><th className="p-3">Action</th></tr></thead><tbody>{fees.length?fees.map(f=>{const outstanding=Math.max(0,Number(f.amount_due)-Number(f.amount_paid));return <tr key={f.id} className="border-b border-slate-100"><td className="p-3 font-black">{formatMonth(f.fee_month)}</td><td className="p-3 font-bold">{money(f.amount_due)}</td><td className="p-3 font-bold text-green-700">{money(f.amount_paid)}</td><td className={`p-3 font-black ${outstanding?'text-red-700':'text-green-700'}`}>{money(outstanding)}</td><td className="p-3">{formatDate(f.payment_date)}</td><td className="p-3">{f.payment_method||"—"}</td><td className="p-3">{f.receipt_number||"—"}</td><td className="p-3"><div className="flex flex-wrap gap-2">{outstanding>0&&<button onClick={()=>{setFeeForm(x=>({...x,feeId:f.id,amount_paid:""}));setShowFeeForm(false)}} className="rounded-lg bg-green-700 px-3 py-2 text-xs font-black text-white">Pay</button>}{Number(f.amount_paid)>0&&<button onClick={()=>printReceipt(f)} className="inline-flex items-center gap-1 rounded-lg border border-slate-200 px-3 py-2 text-xs font-black"><FiPrinter/> Receipt</button>}</div>{feeForm.feeId===f.id&&<div className="mt-3 rounded-xl bg-slate-50 p-3"><div className="grid gap-2 sm:grid-cols-2"><input type="number" min="1" max={outstanding} placeholder={`Amount (max ${outstanding})`} value={feeForm.amount_paid} onChange={e=>setFeeForm(x=>({...x,amount_paid:e.target.value}))} className="rounded-lg border border-slate-200 px-3 py-2"/><input type="date" value={feeForm.payment_date} onChange={e=>setFeeForm(x=>({...x,payment_date:e.target.value}))} className="rounded-lg border border-slate-200 px-3 py-2"/><select value={feeForm.payment_method} onChange={e=>setFeeForm(x=>({...x,payment_method:e.target.value}))} className="rounded-lg border border-slate-200 px-3 py-2"><option>Cash</option><option>UPI</option><option>Bank Transfer</option><option>Cheque</option><option>Other</option></select><input placeholder="Remarks" value={feeForm.remarks} onChange={e=>setFeeForm(x=>({...x,remarks:e.target.value}))} className="rounded-lg border border-slate-200 px-3 py-2"/></div><div className="mt-2 flex gap-2"><button onClick={()=>recordFeePayment(f)} disabled={saving} className="rounded-lg bg-green-700 px-3 py-2 text-xs font-black text-white">Save Payment</button><button onClick={()=>setFeeForm(x=>({...x,feeId:""}))} className="rounded-lg border px-3 py-2 text-xs font-bold">Cancel</button></div></div>}</td></tr>}) : <tr><td colSpan={8} className="p-10 text-center text-slate-400">No monthly fee ledger exists yet. Set the monthly fee above to generate the month-by-month ledger.</td></tr>}</tbody></table></div></section>

      <section id="documents" className="mt-7 scroll-mt-36 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><FiFileText className="text-xl text-green-700"/><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Admission Documents</p><h2 className="text-2xl font-black">Documents Submitted During Admission</h2></div></div><div className="mt-5 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-slate-200 p-5"><p className="font-black">Student Photo</p>{photoUrl?<img src={photoUrl} alt="Student" className="mt-4 h-44 w-36 rounded-xl object-cover"/>:<p className="mt-2 text-sm text-slate-400">No photo available.</p>}</div><div className="rounded-2xl border border-slate-200 p-5"><p className="font-black">Identity Proof {student.student_document_type?`(${student.student_document_type})`:""}</p>{documentUrl?<a href={documentUrl} target="_blank" className="mt-4 inline-flex items-center gap-2 rounded-xl bg-green-700 px-4 py-2.5 font-bold text-white"><FiDownload/> View / Download</a>:<p className="mt-2 text-sm text-slate-400">No identity document available.</p>}</div>{certificateUrl&&<div className="rounded-2xl border border-slate-200 p-5"><p className="font-black">Certificate {student.certificate_type?`(${student.certificate_type})`:""}</p><a href={certificateUrl} target="_blank" className="mt-4 inline-flex items-center gap-2 rounded-xl bg-green-700 px-4 py-2.5 font-bold text-white"><FiDownload/> View / Download</a></div>}</div></section>

      <section id="id-card" className="mt-7 scroll-mt-36 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Identity</p><h2 className="text-2xl font-black">Student ID Card</h2></div><button onClick={printIdCard} className="inline-flex items-center justify-center gap-2 rounded-xl bg-green-700 px-5 py-3 font-black text-white"><FiPrinter/> Print ID Card</button></div><div className="mx-auto mt-6 max-w-4xl overflow-hidden rounded-3xl border border-green-200 shadow-xl"><div className="bg-gradient-to-r from-green-950 via-green-800 to-green-600 p-6 text-white"><p className="text-xs font-black uppercase tracking-[.3em] text-green-200">Madrasa Majmaul Bahrain Bijol</p><p className="mt-1 text-sm text-green-100">A Centre of Islamic Education</p></div><div className="grid gap-6 p-6 md:grid-cols-[180px_1fr]"><div className="flex justify-center">{photoUrl?<img src={photoUrl} alt="Student" className="h-52 w-44 rounded-2xl border-4 border-green-100 object-cover"/>:<div className="flex h-52 w-44 items-center justify-center rounded-2xl bg-green-50 text-5xl font-black text-green-700">{initials}</div>}</div><div><h3 className="text-3xl font-black">{student.student_name}</h3><p className="mt-1 font-black text-green-700">{student.student_id}</p><div className="mt-5 grid gap-3 sm:grid-cols-2"><div><p className="text-xs font-black uppercase text-slate-400">Date of Birth</p><p className="font-bold">{formatDate(student.date_of_birth)}</p></div><div><p className="text-xs font-black uppercase text-slate-400">Course</p><p className="font-bold">{student.course||"—"}</p></div><div><p className="text-xs font-black uppercase text-slate-400">Father / Guardian</p><p className="font-bold">{student.father_name||student.guardian_name||"—"}</p></div><div><p className="text-xs font-black uppercase text-slate-400">Guardian Mobile</p><p className="font-bold">{student.mobile||"—"}</p></div><div><p className="text-xs font-black uppercase text-slate-400">Village / Town</p><p className="font-bold">{student.village_city||"—"}</p></div><div><p className="text-xs font-black uppercase text-slate-400">District / State</p><p className="font-bold">{[student.district,student.state].filter(Boolean).join(" / ")||"—"}</p></div></div></div></div><div className="flex flex-col justify-between gap-2 bg-green-50 px-6 py-4 text-xs font-bold text-slate-600 sm:flex-row"><span>Issued by Madrasa Administration</span><span>Admission Year: {student.admission_year||new Date(student.created_at).getFullYear()}</span></div></div></section>

      <section id="activity" className="mt-7 scroll-mt-36 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><FiActivity className="text-xl text-green-700"/><div><p className="text-xs font-black uppercase tracking-[.2em] text-green-700">Activity</p><h2 className="text-2xl font-black">Student Activity</h2></div></div><div className="mt-5 grid gap-3 md:grid-cols-3"><div className="rounded-2xl bg-slate-50 p-5"><p className="text-xs text-slate-400">Admission Created</p><p className="mt-1 font-black">{formatDate(student.created_at)}</p></div><div className="rounded-2xl bg-green-50 p-5"><p className="text-xs text-green-700">Admission Approved</p><p className="mt-1 font-black">{formatDate(student.approved_at)}</p></div><div className="rounded-2xl bg-blue-50 p-5"><p className="text-xs text-blue-700">Student ID</p><p className="mt-1 font-black">{student.student_id||"—"}</p></div></div></section>
    </section>
  </main>;
}
